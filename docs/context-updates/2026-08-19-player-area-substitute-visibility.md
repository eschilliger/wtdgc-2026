# Player area · substitute visibility

- A linked France player sees every published round in their division, not only rounds where they are in the active six.
- `Mes matchs` labels the player's round status explicitly:
  - `Titulaire` when `roster.selectedPlayerIds` contains the linked `personId`;
  - `Remplaçant` otherwise.
- The player view still exposes no Staff notes or internal opponent scenario data.
